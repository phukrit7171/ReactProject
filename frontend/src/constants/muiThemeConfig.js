// Material UI Theme Configuration
// Defines the application's design system and theme properties
// Import createTheme function from Material UI
import { createTheme } from '@mui/material/styles';

// Define custom theme with application colors and styles
// Export the theme for use throughout the application